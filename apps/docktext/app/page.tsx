import { SiteHome } from "@dock/shared/components";

export default function Home() {
  return (
    <SiteHome
      brandKey="docktext"
      headline="A focused AI workspace for text."
      body="DockText is the writing and cleanup hub in the Dock AI Ecosystem, sharing navigation, footer, UI, dark mode, and SEO patterns with every Dock app."
    />
  );
}
