import { SiteHome } from "@dock/shared/components";

export default function Home() {
  return (
    <SiteHome
      brandKey="dockseo"
      headline="A focused AI workspace for SEO."
      body="DockSEO is the search workflow hub in the Dock AI Ecosystem, sharing navigation, footer, UI, dark mode, and SEO patterns with every Dock app."
    />
  );
}
