import { SiteHome } from "@dock/shared/components";

export default function Home() {
  return (
    <SiteHome
      brandKey="dockimg"
      headline="A focused AI workspace for images."
      body="DockIMG is the image hub in the Dock AI Ecosystem, sharing navigation, footer, UI, dark mode, and SEO patterns with every Dock app."
    />
  );
}
